using UnityEngine;

public class HasBeenClonedComponent : MonoBehaviour
{
    public bool Cloned = true;
    public GameObject Clone;
    // Start is called before the first frame update
    void Start()
    {
        
    }
}
